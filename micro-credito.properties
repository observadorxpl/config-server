spring.data.mongodb.uri=mongodb://${com.bootcamp.mongodb.url}/productos-creditos
spring.application.name=micro-credito
server.port=${PORT:0}
eureka.instance.instance-id=${spring.application.name}:${spring.application.instance_id:${random.value}}
